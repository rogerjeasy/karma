# The agents read from the repo-root .env — see /.env.example for all variables.
# Place your .env at the repo root, not here.
#
# If you need to override a specific value for the agents only, create agents/.env
# with just that variable — it takes precedence over the root .env.
