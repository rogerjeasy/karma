# The API reads from the repo-root .env — see /.env.example for all variables.
# Place your .env at the repo root, not here.
#
# If you need to override a specific value for the API only, create api/.env
# with just that variable — it takes precedence over the root .env.
