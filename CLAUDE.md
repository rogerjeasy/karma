# Karma — Claude Instructions

## Git rules

**Never run `git commit` or `git push` (or any destructive git operation) without the user explicitly asking for it.**

Prepare changes, show diffs, and explain what would be committed — but stop there and wait for instruction.
